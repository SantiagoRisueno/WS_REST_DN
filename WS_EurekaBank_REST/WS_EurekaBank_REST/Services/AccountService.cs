using WS_EurekaBank_REST.Models;

namespace WS_EurekaBank_REST.Services
{
    public class AccountService
    {
        public bool RegisterDeposit(string account, decimal amount)
        {
            return true;
        }
    }
}
